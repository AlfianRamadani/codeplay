import { Ionicons } from '@expo/vector-icons';
import { View } from 'react-native';
export default function TabBarItem({ IconName, IconComponent = Ionicons, size = 26, color, focused, label }) {
    return (
        View
    );
}
